import { Routes } from '@angular/router';

import { NotificationTrayComponent } from './index';

export const NotificationTrayRoutes: Routes = [
  {
    path: 'notification-tray',
    component: NotificationTrayComponent
  }
];
